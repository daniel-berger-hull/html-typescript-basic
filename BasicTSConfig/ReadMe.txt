Use the following tsconfig.json in your TypeScript projects...

It has an 
* input folder (/src) 
* output folder (/public, which is the default folder for the server http-server)

You can change to fit the folder of your projects...
